<ul class="cart__item jsLineItem" data-line-index="{{ index}}">
  <li class="cart__item-img flex-basis-1/3">
    <a href="{{ item.url | within: collection.all}}">
      <img src="{{ item | img_url: 'medium' }}" alt="{{ item.title | escape }}">
    </a>
  </li>
  <li class="cart__item-info flex-1">
    <div class="cart__item-info-general">
      <p class="product-title">
        <a href="{{ item.url}}">{{ item.title }}</a>
      </p>
      <div class="cart__item-options">
        {%- unless item.product.has_only_default_variant %}
          <div class="options">
            {% for option in item.options_with_values -%}
              <p>{{ option.name }}: {{ option.value }}</p>
            {%- endfor %}
          </div>
        {% endunless %}
      </div>
    </div>
    <div class="cart__item-info price">
      <p class="price">price</p>
      <p class="price-compare">price-compare</p>
    </div>
    <div class="cart__item-info qty">
      <div class="cart-item__quantity-wrapper">
        <span class="icon remove__qlt">
          {% render 'icon-remove' %}
        </span>
        <input type="number" value="{{ item.quantity }}" min="1" class="quantity__input" name="quantity">
        <span class="icon add__qlt">
          {% render 'icon-add' %}
        </span>
      </div>
    </div>
    <div class="cart__item-info sub">
      <span>price*qty</span>
    </div>
    <div class="cart__item-info remove">
      <div class="btn-remove-all"><a href="/cart/change?line={{ forloop.index }}&amp;quantity=0">remove</a></div>
    </div>
  </li>
</ul>
